#include <stdio.h>

/**
 * main - Prints a quote fragment.
 * Return: returns an int (termination code)
 */

int main(void)
{
puts("\"Programming is like building a multilingual puzzle");

return (0);
}
