#include <stdio.h>

/**
 * main - Entry point for alphabet printing function
 * Return: 0-status code
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar(10);

	return (0);
}
