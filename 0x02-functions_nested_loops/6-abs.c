#include "holberton.h"

/**
 * _abs - Prints the absolute value of a number
 * @n: number to check the absolute value of
 * Return: absolute value of n
 */
int _abs(int n)
{
	return (n < 0 ? -n : n);
}
