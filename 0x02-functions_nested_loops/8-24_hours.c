#include "holberton.h"

/**
 * jack_bauer - Prints the 24-hr format time
 * Return: void
 */
void jack_bauer(void)
{
	int hr, min;

	for (hr = 0; n < 24; n++)
	{
		for (min = 0; m < 60; m++)
		{
			if ()
			_putchar(':');
			if (minutes < 10)
			{
				_putchar('0');
				_putchar('0' + minutes);
			}
			else
			{
				_putchar('0' + product / 10);
				_putchar('0' + product % 10);
			}
			_putchar('\n');
		}
	}
}
