#include <stdio.h>

/**
 * set_string - Sets the value of a pointer-to-a-char (char*)
 * @s : P
 * @to : Size of the square matricies
 * Return: Void;
 */
void set_string(char **s, char *to)
{
	*s = to;
}
